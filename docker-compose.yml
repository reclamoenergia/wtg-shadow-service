version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: windshadow
      POSTGRES_USER: windshadow
      POSTGRES_PASSWORD: windshadow
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
